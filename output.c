#include <stdio.h>

int main(){
    int age = 23;
    float pi = 3.14;  // the value of pi is in the form of decimal point
    char hashtag = '#';  // character is always written in single quote

// Getting the output of given variable

printf("the age is %d \n", age);
printf("the value is %f \n", pi);
printf("the character is %c \n", hashtag);
return 0;
}